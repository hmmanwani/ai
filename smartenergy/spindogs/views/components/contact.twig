<section class="contact">
    <div class="container">
        <div class="row">
            <div class="col-12">
                {% if fields.contact_form %}
                <div class="contact-form pl-lg-3">
                    {% if fields.form_title %}
                    <h2 class="h2">{{ fields.form_title }}</h2>
                    {% endif %}
                    {{ fields.contact_form }}
                </div>
                {% endif %}
            </div><!-- column -->
        </div><!-- row -->
    </div><!-- container -->
</section>
